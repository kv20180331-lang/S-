# 学生流程验收记录

日期：2026-09-09

## 静态检查

- `node --check app.js`：通过
- `node --check data.js`：通过
- `node --check reporting-data.js`：通过

## 浏览器完整流程

使用 Chromium + Playwright，将当前HTML/CSS/JS完整内联运行，模拟普通学生操作：

1. Cover进入Briefing。
2. 建立2人编辑部并覆盖四项职责。
3. 09:00主动点击地图发现信息。
4. 测试左栏“全部/追踪/待核/确认”筛选。
5. 提交09:00编辑会。
6. 进入13:00重新发现地点更新并执行调查。
7. 提交13:00编辑会。
8. 发布午间快讯。
9. 进入17:00再次查询更新。
10. 通过截稿可行性检查并进入DEADLINE。
11. DEADLINE后连续返回地图，点击截稿前未打开地点：已发现线索数量不增加。
12. 进入采写室并选择：消息=强降雨、通讯=电梯、特写=夜市、评论=机器人。
13. 验证深采动作执行前不显示采访结果摘要。
14. 通讯取得2个人物采访+背景后达到最低写作条件并提交。
15. 验证写作页返回通讯采写台，而不是沿历史连续倒带。
16. 特写取得基础现场+人物跟随后提交。
17. 评论取得基础观点+两项深入材料后提交。
18. 消息导语可从超过5条的全部已发现事实中选择并提交。
19. 四篇完成后进入最终版面会议，没有第二套头条/副稿写作。
20. 选择头条、6条简讯、3条不刊发线索并发布。
21. Published正常显示四文体成果，无旧版TOP STORIES重复主稿。

结果：

- 最终测试组发现59条线索。
- 四篇作品均正式提交。
- 浏览器console error：0。
- page error：0。

## 额外验证

- 普通学生模式教师控制隐藏。
- 成员填写姓名但未分配任何职责时，不能进入09:00。


## 轻度修改验收：无文字输入也能推进

本轮修改重点不是改变教学规则，而是取消所有自由文字输入的强制校验。代码层已确认：

- `app.js` 中不存在 HTML `required` 属性；
- 不再使用“正文最少字数”“必须勾选5项事实核对”“必须填写编辑部姓名/职责”等硬性校验阻断提交；
- 编辑会文字问题可空；13:00 未选择“是否改变判断”时默认按“否”保存；
- 午间快讯空标题/导语/来源会使用已有新闻线索回填；
- 四文体可以在正文、标题、计划均为空的情况下提交；
- 深采完整度提示仍显示，但不再禁用“进入写作”；
- 空白稿在最终版面有安全回退标题与“正文未填写”提示。

保留的仍是“选择型”课堂动作与结构规则，不要求学生键入文字即可推进。
