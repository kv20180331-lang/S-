# 《S城新闻沙盘》完整交接说明｜学生流程收口版

## 给 Codex / 新对话的第一条指令

请先完整读取本目录全部源码，再修改。不要根据本说明猜测实现，不要重搭项目，不要引入框架、后端、数据库或登录。

本项目是八年级语文“新闻单元”互动教学网页。技术栈：HTML + CSS + 原生 JavaScript，静态运行。

## 文件

- `index.html`：入口；加载全部样式与脚本。
- `style.css`：基础三栏、地图、线索流、详情。
- `enhancement.css`：视觉增强。
- `v1.css`：新闻日、编辑会、快讯、出版、Review基础样式。
- `desk-v2.css`：地图优先架构、特殊入口、新闻日筛选、截稿状态。
- `reporting.css`：采写室、四文体写作、最终版面会议。
- `data.js`：34地点、市场快照、105条新闻日底层事件。
- `reporting-data.js`：深度采写故事与采访/现场/背景/观点/数据材料。
- `app.js`：全部状态、场景、发现、采写、写作、出版、localStorage。

样式顺序不要改变：

`style.css → enhancement.css → v1.css → desk-v2.css → reporting.css`

脚本顺序不要改变：

`data.js → reporting-data.js → app.js`

## 当前完整学生流程

```text
Cover
→ Briefing（建立2—3人编辑部）
→ 09:00 新闻日
→ 09:00 编辑会
→ 13:00 新闻日
→ 13:00 编辑会
→ 午间快讯
→ 17:00 新闻日
→ DEADLINE
→ S城采写室
→ 四篇作品计划
→ 消息 / 通讯 / 新闻特写 / 评论采写与写作
→ 最终版面会议
→ Published《S城日报》
→ Review
```

### Briefing

2—3人一组；成员1、2必填，成员3可选。四项职责：

- 主编 / 选题
- 记者 / 调查
- 核实
- 文字 / 版面

同一成员可兼任。每个已填写成员至少承担1项职责，四项职责全组必须覆盖。

### 新闻日

- 09:00 +3采访机会
- 13:00 +3
- 17:00 +2
- 全组共享，最多8次
- 地图是唯一主要信息入口
- 新轮次只在地点显示 NEW，不自动把信息推入左栏

左栏 `DISCOVERED WIRE` 只显示本组实际发现的线索，并有轻量筛选：

- 全部
- ★追踪
- ？待核
- ✓确认

筛选不会改变数据，只改善大量线索下的回看。

### 编辑会

09:00：选3条继续追踪，1条头条候选，写最想弄清的问题。

13:00：重新判断。这里“改变判断的新证据”按**本组在13:00轮次实际发现的时间**判断，而不是按事件自身发布轮次判断；即便某条上午事件到13:00才被学生第一次发现，也可以被选为新证据。

头条来源提示按不同来源数量判断，不再简单以同一故事卡片数量冒充交叉验证。

## DEADLINE 与防卡死机制

17:00学生点击截稿时，系统先检查后续任务是否可完成：

1. 四种文体都至少有一个本组已解锁故事可选；
2. 存在满足“四篇至少覆盖3个故事、同一故事最多用于2种文体”的组合；
3. 本组至少发现9条线索，以满足最终6—10条简讯 + 至少3条不刊发线索的基础编排需求。

条件不足时不锁死新闻日，只提示缺口，学生继续发现信息。

一旦正式 DEADLINE：

- `deadlineLocked = true`
- 返回地图只能回看
- 未打开地点不能再获取信息
- 已打开地点不会再发现新事件
- 采访按钮消失 / 调查禁止
- 特殊政务、市场、电讯入口不再新增信息
- 编辑标记锁定
- 顶部显示“新闻日已截稿 🔒”而不是剩余采访次数

## S城采写室

新闻日结束后，从本组已发现故事中选择：

- 消息1则
- 通讯1则
- 新闻特写1则
- 评论1则

四篇至少3个不同故事，同一故事最多进入2种文体。

新闻日阶段不显示“适合什么文体”；进入采写室后，系统才按文体列出本组具备采写条件的故事。

### 不替学生做选题

采写室故事标题和提示已改成中性表达。故事卡只提示可继续获取的**材料类型**，例如：

`人物采访 · 现场观察 · 背景资料 · 数据资料 · 多方观点`

不再直接告诉学生“应该讨论什么”“核心问题是什么”。

### 深采动作不剧透

主动采访 / 查询按钮执行前只显示行动名称、材料性质，不显示结果摘要。

点击以后，采访结果才进入 `REPORTER'S CLIPBOOK`。

### 独立深采额度

不消耗新闻日8次采访：

- 消息：0次额外深采
- 通讯：最多4次主动深采
- 特写：基础现场自动获得 + 1次人物跟随
- 评论：基础事实和不同观点自动获得 + 最多2次深入材料

### 写作最低条件

系统用“已达到最低写作条件”，不再使用容易误导的“材料完整”。

通讯：
- 至少2个人物采访
- 至少1份背景/数据
- 有事实发展过程

特写：
- 明确现场
- 明确时点/地点
- 至少3个可观察细节
- 1个人物跟随

评论：
- 至少3项事实依据
- 至少2种不同观点
- 至少1份政策/数据/第三方材料

### 消息导语

导语核心事实可以从该故事**全部本组已发现事实**中选择，不再只显示最新5条。

页面明确提示：

> 系统按时间排序，但“最新”不等于“最重要”。

### 真实性规则

所有写作台固定强调：可以选择、组织、转述事实，但不能创造事实。

提交前5项 FACT CHECK 中最后一项为：

> 没有自行补写素材中未出现的人物心理、动作、细节或因果关系。

即：允许使用系统现场笔记已有动作与细节，不允许自行虚构。

## 返回逻辑

新闻日仍保留内部跳转历史数据，但学生点击“返回”时使用明确父级关系，不再沿完整 `sceneHistory` 一页页倒带。

采写阶段固定层级：

```text
写作页 → 当前文体采写页
文体采写页 → 四篇作品计划
四篇作品计划 → 采写室首页
采写室首页 → DEADLINE
```

这样不会出现“评论 → 计划 → 特写 → 计划 → 通讯……”连续倒带。

## 最终出版逻辑（已删除重复写作）

四篇正式提交后，不再重新写旧版“1篇头条 + 2篇副稿”。

进入 `FINAL EDITION CONFERENCE` 只做编辑选择：

1. 从消息 / 通讯 / 新闻特写中选1篇作为首页头条；
2. 评论固定进入 `OPINION`；
3. 从实际发现线索中选6—10条今日简讯；
4. 至少选3条“我们决定不报道”并记录原因。

最终《S城日报》主稿就是前面已经写完的四篇作品。

旧 `finalEdition` 三主稿结构仍保留兼容渲染，避免旧localStorage直接报错；新结构使用：

`finalEdition.mode === "reporting"`

最终首页导航只显示实际存在的栏目，避免点击空锚点。

## 教师模式

学生正常打开页面看不到教师控制。

教师需要导演模式时使用：

`index.html?teacher=1`

教师模式仍为本机控制，不同步学生设备。

## localStorage

键：

`s-city-newsroom-v1`

必须继续兼容旧数据。不要主动清空学生已有：

- 已发现线索
- 会议snapshot
- 采访历史
- 午间快讯版本
- 编辑标记历史
- 采写室材料
- 四篇稿件
- 最终版面

只有用户点击“重新开始新闻日”才清空。

## 数据原则

- `data.js` 的105条底层EVENTS不要为了UI修改而重写。
- 深采材料维护在 `reporting-data.js`。
- 固定采访引语不要自行润色。
- 模拟市场数据继续标明“模拟市场数据，仅用于新闻教学”。
- 人物不得提前知道未来时点才公布的信息。
- 评论材料必须有不同观点，不自动生成学生立场。
- 不加入AI写作、AI评分或AI事实核查。

## 本版额外修正（学生从头走流程后）

本版已一次性处理：

1. DEADLINE后地图新增信息漏洞。
2. 四篇写完后再次写3篇主稿的重复劳动。
3. 最终首页旧三主稿压过四文体成果的问题。
4. Briefing任务与实际课程流程不一致。
5. 深采按钮执行前泄露采访结果。
6. “材料完整”误导性措辞。
7. 采写室顶部仍显示新闻日剩余采访次数。
8. 学生可见教师控制。
9. 系统直接给学生报道角度/文章提纲。
10. 采写阶段返回按钮连续倒带。
11. 消息导语只能从最新5条选。
12. FACT CHECK“动作/细节”表述容易误解。
13. 左栏线索过多后缺少轻量筛选。
14. 学生可能在材料不足时截稿、随后永久卡死。
15. 填写了姓名却没有职责也能开始的角色校验漏洞。
16. 13:00“新证据”错误地只按事件round判断。
17. 头条核实提示把同故事卡片数误当成独立来源数。
18. 最终日报导航可能链接到不存在栏目。

## 已执行测试

- `node --check app.js` 通过
- `node --check data.js` 通过
- `node --check reporting-data.js` 通过
- 使用 Chromium + Playwright 内联运行完整学生流程测试通过：
  - Cover → Briefing
  - 09:00 / 13:00 / 17:00
  - 两次编辑会
  - 午间快讯
  - DEADLINE
  - 截稿后返回地图不能新增信息
  - 四篇作品选题
  - 通讯深采与写作
  - 特写深采与写作
  - 评论深采与写作
  - 消息全部事实导语选择与写作
  - 最终版面会议
  - Published
- 测试中最终发现59条线索，四篇均正式提交，浏览器无JavaScript console/page error。
- 单独测试：学生模式教师控制隐藏；已填写成员无职责不能开始。

如果继续开发，请优先做真实课堂试用后的微调，不要再次增加一套平行出版流程。


## 2026-09-09 轻度修改：所有文字输入改为选填

当前版本增加“可直接推进”模式：

- 编辑部名称、成员姓名与职责不再作为进入新闻日的硬性门槛；可留空开始。
- 09:00 / 13:00 编辑会中的文字说明均为选填；未填写时也可以提交。
- 午间快讯的标题、导语、来源可留空，系统会沿用所选线索；若连线索也未选，则使用当前已发现线索作为回退，仅用于保证流程可推进。
- 快讯更新/更正的文字可留空，留空时沿用上一版本。
- 通讯/特写/评论采写材料即使未达到建议完整度，也允许先进入写作台；完整度仍作为提示保留，不再是硬性阻断。
- 四种文体的写作计划、标题、正文、事实核对勾选均改为选填；空白稿也可提交并继续流程。
- 空白稿在版面中自动用所属故事名称作为显示标题，并显示“正文未填写”，避免空白版面或报错。
- 所有核心“选择型”教学动作仍保留，例如新闻日发现线索、编辑会选择追踪线索、四文体选题、最终简讯与不报道选择。这些不是文字填写项。

本修改不改变原有数据、新闻日采访额度、DEADLINE锁定、深采素材、四文体结构、最终版面和localStorage结构。
